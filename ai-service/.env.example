# ── Python AI Service environment variables ───────────────────────────────────
# Copy to .env and activate before running `uvicorn src.main:app`.

SBERT_MODEL=all-MiniLM-L6-v2
REDIS_URL=redis://localhost:6379
CHROMA_PATH=./chroma_db
SPRING_CALLBACK_URL=http://localhost:8080
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
PDF_STORAGE_DIR=./reports

# Claude API key — required for XAI natural language justifications
ANTHROPIC_API_KEY=
